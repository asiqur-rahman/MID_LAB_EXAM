<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/Employee" method="">
					<table>
						<tbody>
                <% for(var i=0; i < label.length; i++) { %>
                  <tr>
                  <td><%= label[i]%></td>
                  <!-- <td><%= data[i] %></td> -->
                  <td><input type="text" name="num<%=i%>" value="<%= data[i] %>" required></td>
                 </tr>
                <% } %>
                <tr>
                  <td><%=msg%></td>
                </tr>
                <tr>
									<td></td>
                  <td><input type="submit" name="" value="Ok" style="background: #ff523b; border-radius:40px; color:white;"></td>
                  <td></td>
                </tr>
						</tbody>
					</table>
          </form>
  </body>
</html>
